DEF ZONE_LEN       EQU 8 ; Length of a zone definition in bytes (group, map, x1, y1, x2, y2, id, environment)
DEF ZONE_GROUP EQU $FF ; A zone counts as a map in this group for the encounter tables

; Zone ID
DEF ZONE_VIRIDIAN_FOREST EQU 1
